services:
  - type: web
    name: searxng
    env: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /healthz
    envVars:
      - key: SEARXNG_BASE_URL
        sync: false
      - key: SEARXNG_SECRET
        generateValue: true
